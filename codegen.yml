overwrite: true
documents: null
schema: http://localhost:4000/graphql
generates:
  ./src/fragmentTypes.json:
    plugins:
      - fragment-matcher
  ./schema-types.d.ts:
    plugins:
      - typescript
  ./graphql.schema.json:
    plugins:
      - introspection
