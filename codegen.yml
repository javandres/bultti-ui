overwrite: true
documents: null
schema: http://localhost:4100/graphql
generates:
  ./src/fragmentTypes.json:
    plugins:
      - fragment-matcher
  ./src/schema-types.ts:
    plugins:
      - typescript
  ./src/graphql.schema.json:
    plugins:
      - introspection
